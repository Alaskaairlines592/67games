# 67games
The site with 1000 games.

All of the games are now served directly from the main `index.html` using `games.json`.  The previous external page under `games/algebra-is-fun.github.io` is no longer linked and can be deleted if not needed.
